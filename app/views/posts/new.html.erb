<h1>Create a New Post</h1>

<%= render :partial => 'layouts/errors', :locals => { :object => @post } %>

<%= form_for @post do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :url %>
  <%= f.text_field :url %>
  <%= f.submit %>
<% end %>